<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudfitness.mapper.MembershipMapper">
    
    <resultMap id="BaseResultMap" type="com.cloudfitness.entity.Membership">
        <id column="membership_id" property="membershipId"/>
        <result column="user_id" property="userId"/>
        <result column="membership_type" property="membershipType"/>
        <result column="start_date" property="startDate"/>
        <result column="expiry_date" property="expiryDate"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectCurrentByUserId" resultMap="BaseResultMap">
        SELECT * FROM memberships
        WHERE user_id = #{userId}
        AND expiry_date &gt;= NOW()
        ORDER BY expiry_date DESC
        LIMIT 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM memberships
        WHERE user_id = #{userId}
        ORDER BY expiry_date DESC
    </select>

</mapper>

